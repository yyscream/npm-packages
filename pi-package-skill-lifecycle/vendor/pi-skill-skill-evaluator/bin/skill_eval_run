#!/usr/bin/env bash
set -euo pipefail
SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
PACKAGE_DIR="$(cd "$(dirname "$SCRIPT_PATH")/.." && pwd)"
exec python3 "$PACKAGE_DIR/skills/skill-evaluator/scripts/skill_eval.py" run "$@"
