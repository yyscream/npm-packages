# nix store

The Nix store stores immutable build outputs under `/nix/store`.

## Garbage collection

Garbage collection removes unreachable store paths. Be careful with destructive cleanup commands.
