# Packages

Nixpkgs provides packages and the `environment.systemPackages` option for NixOS systems.

## environment.systemPackages

Add packages to `environment.systemPackages` in `configuration.nix` for system-wide availability.
